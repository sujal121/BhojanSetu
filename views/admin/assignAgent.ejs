<main>
	<%- include('../partials/adminSidebar') %>
	
	<div id="main-wrapper">
		
		<div class="bg-white shadow-sm p-3">
			<span class="me-3" id="sidebar-toggler-btn"><i class="fas fa-bars"></i></span>
			<h5 class="m-0 color-theme d-inline-block title">Assign Ngo</h5>
		</div>
		
		<form action="/admin/donation/assign/<%= donation._id %>" method="POST" class="border m-4 my-3 p-4 bg-white rounded-2 shadow">
			
			<div class="row">
				<div class="mb-4 col-sm-4">
					<label for="donor" class="form-label form-label1">Donor name</label>
					<input type="text" class="form-control form-control1" id="donor" value="<%= donation.donor.firstName + ' ' + donation.donor.lastName %>" disabled>
				</div>
				
				<div class="mb-4 col-sm-8">
					<label for="address" class="form-label form-label1">Address</label>
					<input type="text" class="form-control form-control1" id="address" value= "<%= donation.address %>" disabled>
				</div>
			</div>
			
			<div class="mb-4">
				<label for="agent" class="form-label form-label1">Select Ngo</label>
				<select name="agent" class="form-select form-control1" id="agent">
					<% for(let i=0; i<agents.length; i++) { %>
						<option value="<%= agents[i]._id %>"><%= agents[i].firstName + " " + agents[i].lastName %></option>
					<% } %>
				</select>
			</div>
			
			<div class="mb-4">
				<label for="msg" class="form-label form-label1">Message to Ngo</label>
				<textarea type="text" name="adminToAgentMsg" class="form-control form-control1" id="msg" placeholder="You can write here some msg.."></textarea>
			</div>
			
			<button type="submit" class="btn btn2 w-75 d-block m-auto mt-4">Assign</button>
		</form>
		
	</div>
</main>